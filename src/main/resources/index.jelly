<?jelly escape-by-default='true'?>
<div>
    Allows unauthenticated access to check ability to run new jobs at <code>/anonymousUptimeCheck</code>.
    <p>(returns 503 status if Jenkins is in quieting down)</p>
</div>
